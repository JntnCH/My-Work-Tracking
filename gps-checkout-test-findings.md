# GPS Check-out Test Findings

วันที่ทดสอบ: 2026-08-18 (Asia/Bangkok)

## ขั้นตอนที่ผ่านแล้ว

1. เปิด production build ล่าสุดบน `http://127.0.0.1:10000/` ใน browser
2. จำลอง `navigator.geolocation.getCurrentPosition()` ให้คืนพิกัดสองชุดที่แตกต่างกัน โดยชุดแรกคือ `13.756331, 100.501762`
3. กดค้นหาตำแหน่งและพบข้อความ `Lat: 13.756331, Lng: 100.501762`
4. กด Check-in สำเร็จ และหน้าแอปแสดงสถานะกำลังทำงานพร้อมสถานที่จาก reverse geocode

## ขั้นตอนถัดไป

กด Check-out เพื่อยืนยันว่าระบบเรียก geolocation ครั้งที่สองและใช้พิกัดชุดที่สอง `13.746000, 100.534000` ไม่ใช้พิกัด Check-in เดิม

## การเปลี่ยนแปลงที่ตรวจสอบ

`CheckInPanel.tsx` เปลี่ยน `fetchGPS` ให้คืน `Promise<GPSPoint | null>` และ `doCheckOut` รอผลพิกัดใหม่ก่อนเรียก `onCheckOut`. หากไม่มีพิกัด ระบบจะไม่บันทึก Check-out และแสดงข้อความผิดพลาด.

## ผลการตรวจโค้ดที่เกี่ยวข้อง

`use-work-tracker.ts` เก็บพิกัดออกใน `checkOutGPS`, `saveDBWorkLog` ส่งลงคอลัมน์ `check_out_gps`, และ `logToRow` แยกคอลัมน์พิกัดเข้า/ออกใน Google Sheets.

## Production instance ใหม่หลังแก้ไข

เปิด instance ใหม่บน `http://127.0.0.1:10001/` เพื่อหลีกเลี่ยง server/cache เดิม และพบว่า artifact มีข้อความ/handler ของการดึง GPS ใหม่แล้ว

การทดสอบ Check-in ผ่าน: mock GPS ครั้งแรกคืน `13.756331, 100.501762` และหน้าแอปแสดงพิกัดดังกล่าวก่อนและหลัง Check-in

กำลังทดสอบขั้นตอน Check-out โดยคาดว่าการเรียกครั้งถัดไปจะคืน `13.746000, 100.534000` และถูกบันทึกเป็น `checkOutGPS`

## ผลทดสอบยืนยันสำเร็จ

ใน production instance ใหม่ เมื่อกด Check-out ระบบเปลี่ยนพิกัดบนหน้าเป็น `13.746000, 100.534000` และแสดงที่อยู่ `พาร์ค พารากอน, เขตปทุมวัน, กรุงเทพมหานคร`

ตรวจสอบ localStorage หลังบันทึกพบว่า:

- `gpsCalls: 2`
- `checkInGPS: 13.756331, 100.501762` — วงเวียนอนุสาวรีย์ประชาธิปไตย
- `checkOutGPS: 13.746000, 100.534000` — พาร์ค พารากอน

จึงยืนยันได้ว่า Check-out เรียก GPS ใหม่จริงและบันทึกพิกัดออกแยกจากพิกัดเข้าได้ถูกต้อง
